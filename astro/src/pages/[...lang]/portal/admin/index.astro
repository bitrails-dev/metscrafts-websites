---
import PortalLayout from "../../../../layouts/PortalLayout.astro";
import PortalAdmin from "../../../../components/portal/PortalAdmin.vue";
import { getStrings } from "../../../../i18n";

const lang: "ar" | "en" = Astro.params.lang === "en" ? "en" : "ar";
const strings = getStrings(lang);
---
<PortalLayout
  lang={lang}
  strings={strings}
  title={`${strings.portal.admin.title} | ${strings.site.name}`}
  description={strings.portal.admin.description}
>
  <PortalAdmin lang={lang} strings={strings} client:load />
</PortalLayout>
