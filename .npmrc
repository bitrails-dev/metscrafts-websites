# Windows AV/indexer locks pnpm's virtual-store temp→rename step (ERR_PNPM_EPERM),
# which breaks the default isolated linker on this machine. Hoisted builds a flat
# node_modules and skips that rename. Safe to drop if installing on a machine
# without the lock issue.
node-linker=hoisted
